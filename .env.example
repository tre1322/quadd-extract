# Quadd Extract Environment Variables

# Required: Anthropic API key for Claude Vision
ANTHROPIC_API_KEY=sk-ant-your-key-here

# Required: JWT Secret for authentication
# Generate with: python -c 'import secrets; print(secrets.token_urlsafe(32))'
# IMPORTANT: Use a secure random string in production, not this placeholder
JWT_SECRET=your-secure-random-string-here

# Server Port (Railway will set this automatically)
PORT=8000

# Database Path (Railway persistent storage)
# For Railway with volume mounted at /app/data
DATABASE_PATH=/app/data/quadd_extract.db

# CORS Allowed Origins (comma-separated)
# For production, set to your Railway domain
# Example: https://your-app.up.railway.app
ALLOWED_ORIGINS=*

# Optional: Model to use (default: claude-sonnet-4-20250514)
CLAUDE_MODEL=claude-sonnet-4-20250514

# Optional: Storage path for uploads
STORAGE_PATH=./data

# Optional: Log level
LOG_LEVEL=INFO
