{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "nixpacks"
  },
  "deploy": {
    "startCommand": "python init_auth.py; uvicorn src.api.main:app --host 0.0.0.0 --port ${PORT:-8000}",
    "restartPolicyType": "on_failure",
    "restartPolicyMaxRetries": 3
  }
}
